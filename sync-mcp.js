/**
 * ⚠️ OBSOLETE - Ce fichier n'est plus nécessaire
 * 
 * Les serveurs MCP sont maintenant directement configurés dans .claude.json
 * par les scripts d'installation (install.ps1 et install.sh).
 * 
 * La clé API est automatiquement mise à jour dans les deux fichiers par le wrapper
 * si nécessaire, donc aucune synchronisation manuelle n'est requise.
 * 
 * Vous pouvez supprimer ce fichier en toute sécurité.
 * 
 * @deprecated
 * @see install.sh
 * @see install.ps1
 */

console.log('[INFO] sync-mcp.js is obsolete. MCP servers are now configured in .claude.json by the installer.');
console.log('[INFO] You can safely delete this file.');
